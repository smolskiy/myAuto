dist
dist-pages
dev-dist
coverage
playwright-report
test-results
package-lock.json
docs

# Справочник машин — данные одной строкой (≈260 КБ), не форматируем.
src/domain/carCatalog.json

# Справочник СТО — данные одной строкой, не форматируем.
src/domain/directory/*.json
